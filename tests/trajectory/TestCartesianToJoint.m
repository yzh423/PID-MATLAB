classdef TestCartesianToJoint < matlab.unittest.TestCase
    methods (Test)
        function reconstructsPathWithContinuousDifferentialMotion(testCase)
            robot = rrm.config.makeRobot("baseline");
            path = rrm.trajectory.cartesianQuintic( ...
                [0.55;0.12],[0.22;0.48],3,0.01,4);
            [solutions,reachable] = rrm.kinematics.inverse( ...
                robot,path.position(:,1));
            testCase.verifyTrue(reachable);

            reference = rrm.trajectory.cartesianToJoint( ...
                robot,path,solutions(:,1));

            sampleCount = numel(path.time);
            positionError = zeros(1,sampleCount);
            velocityError = zeros(1,sampleCount);
            accelerationError = zeros(1,sampleCount);
            for sample = 1:sampleCount
                q = reference.q(:,sample);
                dq = reference.dq(:,sample);
                positionError(sample) = norm( ...
                    rrm.kinematics.forward(robot,q)- ...
                    path.position(:,sample));
                J = rrm.kinematics.jacobian(robot,q);
                Jdot = rrm.kinematics.jacobianDot(robot,q,dq);
                velocityError(sample) = norm( ...
                    J*dq-path.velocity(:,sample));
                accelerationError(sample) = norm( ...
                    J*reference.ddq(:,sample)+Jdot*dq- ...
                    path.acceleration(:,sample));
            end
            testCase.verifyLessThan(max(positionError),1e-10);
            testCase.verifyLessThan(max(velocityError),1e-10);
            testCase.verifyLessThan(max(accelerationError),1e-9);
            testCase.verifyLessThan( ...
                max(abs(diff(reference.q,1,2)),[],"all"),deg2rad(1));
            testCase.verifyGreaterThanOrEqual( ...
                reference.q,robot.jointLimits(:,1)-1e-12);
            testCase.verifyLessThanOrEqual( ...
                reference.q,robot.jointLimits(:,2)+1e-12);
            testCase.verifyGreaterThan(reference.q(2,:),0);
            testCase.verifyEqual(reference.cartesian,path);
        end

        function reportsUnreachableJointLimitAndSingularPaths(testCase)
            robot = rrm.config.makeRobot("baseline");
            unreachable = rrm.trajectory.cartesianQuintic( ...
                [2;0],[2;0],0.1,0.1,0.1);
            testCase.verifyError(@() rrm.trajectory.cartesianToJoint( ...
                robot,unreachable,[0;0]), ...
                "rrm:trajectory:UnreachableCartesianPoint");

            reachable = rrm.trajectory.cartesianQuintic( ...
                [0.55;0.12],[0.55;0.12],0.1,0.1,0.1);
            narrowRobot = robot;
            narrowRobot.jointLimits = deg2rad([-10 10;-10 10]);
            testCase.verifyError(@() rrm.trajectory.cartesianToJoint( ...
                narrowRobot,reachable,[0;0]), ...
                "rrm:trajectory:JointLimitViolation");

            singular = rrm.trajectory.cartesianQuintic( ...
                [robot.L1+robot.L2;0], ...
                [robot.L1+robot.L2;0],0.1,0.1,0.1);
            testCase.verifyError(@() rrm.trajectory.cartesianToJoint( ...
                robot,singular,[0;0]), ...
                "rrm:trajectory:SingularCartesianPath");
        end

        function rejectsMalformedPath(testCase)
            robot = rrm.config.makeRobot("baseline");
            malformed = struct("time",[0;0.1], ...
                "position",zeros(2,2));
            testCase.verifyError(@() rrm.trajectory.cartesianToJoint( ...
                robot,malformed,[0;0]), ...
                "rrm:trajectory:InvalidCartesianPath");
            valid = rrm.trajectory.cartesianQuintic( ...
                [0.55;0.12],[0.55;0.12],0.1,0.1,0.1);
            testCase.verifyError(@() rrm.trajectory.cartesianToJoint( ...
                robot,valid,[0 0]), ...
                "rrm:trajectory:InvalidCartesianPath");
        end
    end
end
