classdef TestCartesianWaypoints < matlab.unittest.TestCase
    methods (Test)
        function composesExactContinuousWaypointPath(testCase)
            points = [0.55 0.45 0.45 0.22; ...
                0.12 -0.02 0.32 0.48];

            path = rrm.trajectory.cartesianWaypoints( ...
                points,[1.4;1.2;1.5],[0.4;0;0.8],0.001);

            testCase.verifyEqual(diff(path.time), ...
                0.001*ones(numel(path.time)-1,1),AbsTol=1e-14);
            testCase.verifyEqual(path.time(end),5.3,AbsTol=1e-14);
            testCase.verifySize(path.time,[5301 1]);
            testCase.verifyEqual(path.arrivalIndices,[1401;3001;4501]);
            testCase.verifyEqual(path.dwellEndIndices,[1801;3001;5301]);
            for waypoint = 1:3
                arrival = path.arrivalIndices(waypoint);
                dwellEnd = path.dwellEndIndices(waypoint);
                testCase.verifyEqual(path.position(:,arrival), ...
                    points(:,waypoint+1),AbsTol=1e-12);
                testCase.verifyEqual(path.position(:,dwellEnd), ...
                    points(:,waypoint+1),AbsTol=1e-12);
                testCase.verifyEqual(path.velocity(:,arrival), ...
                    [0;0],AbsTol=1e-10);
                testCase.verifyEqual(path.acceleration(:,arrival), ...
                    [0;0],AbsTol=1e-9);
            end
            pickupHold = path.arrivalIndices(1):path.dwellEndIndices(1);
            testCase.verifyEqual(path.position(:,pickupHold), ...
                repmat(points(:,2),1,numel(pickupHold)),AbsTol=1e-12);
        end

        function rejectsInvalidWaypointDefinitions(testCase)
            validPoints = [0.5 0.4;0.1 0.3];
            testCase.verifyError(@() rrm.trajectory.cartesianWaypoints( ...
                validPoints(:,1),1,0,0.1), ...
                "rrm:trajectory:InvalidWaypointPath");
            testCase.verifyError(@() rrm.trajectory.cartesianWaypoints( ...
                validPoints,[1;1],0,0.1), ...
                "rrm:trajectory:InvalidWaypointPath");
            testCase.verifyError(@() rrm.trajectory.cartesianWaypoints( ...
                validPoints,1,-0.1,0.1), ...
                "rrm:trajectory:InvalidWaypointPath");
            testCase.verifyError(@() rrm.trajectory.cartesianWaypoints( ...
                validPoints,0.95,0,0.1), ...
                "rrm:trajectory:InvalidWaypointPath");
            testCase.verifyError(@() rrm.trajectory.cartesianWaypoints( ...
                validPoints,1,0,0), ...
                "rrm:trajectory:InvalidWaypointPath");
        end
    end
end
